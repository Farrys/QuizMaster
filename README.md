# QuizMaster - Сервис создания квизов и тестов

## 📋 Описание проекта

QuizMaster - это современный веб-сервис для создания, публикации и прохождения интерактивных квизов и тестов. Приложение построено на чистом JavaScript, HTML5 и CSS3 без использования фреймворков, что обеспечивает высокую производительность и простоту развертывания.

## ✨ Основные возможности

### Для создателей квизов:
- **Конструктор квизов** с drag-and-drop интерфейсом
- **4 типа вопросов**: одиночный выбор, множественный выбор, текстовый ввод, перетаскивание
- **Управление квизами**: создание, редактирование, удаление, копирование
- **Публикация** квизов по уникальной ссылке
- **Детальная статистика** прохождений
- **Система категорий** для организации тестов
- **Экспорт результатов** в различных форматах

### Для прохождения тестов:
- Интуитивный интерфейс прохождения
- Визуальный прогресс-бар
- Таймер (опционально)
- Мгновенная проверка ответов
- Детальные результаты с разбором ошибок

### Технические особенности:
- ✅ Полностью адаптивный дизайн
- ✅ Работает без сервера (localStorage)
- ✅ Современный UI с анимациями
- ✅ Темная тема
- ✅ Валидация форм
- ✅ Toast-уведомления
- ✅ Безопасность данных

## 🚀 Быстрый старт

### Вариант 1: Локальное развертывание

1. **Скачайте все файлы проекта**
   ```bash
   # Структура проекта:
   quiz-master/
   ├── index.html
   ├── styles.css
   ├── app.js
   └── README.md
   ```

2. **Откройте index.html в браузере**
   - Просто дважды кликните на файл index.html
   - Или используйте локальный сервер (рекомендуется)

3. **Готово!** Приложение работает полностью автономно

### Вариант 2: Запуск с локальным сервером (рекомендуется)

```bash
# С Python
python -m http.server 8000

# С Node.js (npx)
npx http-server -p 8000

# С PHP
php -S localhost:8000
```

Затем откройте браузер: `http://localhost:8000`

### Вариант 3: Развертывание на хостинге

#### GitHub Pages:
1. Создайте репозиторий на GitHub
2. Загрузите все файлы
3. Включите GitHub Pages в настройках репозитория
4. Ваш сайт будет доступен по адресу: `https://username.github.io/repository-name`

#### Netlify:
1. Перетащите папку с файлами на netlify.com
2. Получите уникальный URL
3. Опционально: настройте свой домен

#### Vercel:
```bash
npm i -g vercel
vercel --prod
```

## 📱 Использование

### Первый запуск

При первом запуске доступны **демо-данные**:
- **Email**: demo@quizmaster.com
- **Пароль**: demo123

Или создайте новый аккаунт через кнопку "Регистрация"

### Создание квиза

1. **Войдите в систему** или зарегистрируйтесь
2. Нажмите **"Создать квиз"**
3. Заполните **основную информацию**:
   - Название квиза
   - Описание
   - Категория
   - Время прохождения (опционально)

4. Добавьте **вопросы**:
   - Нажмите на кнопку типа вопроса
   - Введите текст вопроса
   - Добавьте варианты ответов
   - Отметьте правильные ответы

5. **Настройте параметры**:
   - Перемешивание вопросов
   - Показ результатов
   - Повторное прохождение

6. **Сохраните** как черновик или сразу опубликуйте

### Типы вопросов

#### 1. Одиночный выбор
- Один правильный ответ
- Классические тесты
- Экзаменационные вопросы

#### 2. Множественный выбор
- Несколько правильных ответов
- Проверка комплексных знаний
- Расширенная валидация

#### 3. Текстовый ввод
- Свободный ответ
- Точное соответствие
- Проверка терминологии

#### 4. Перетаскивание (в разработке)
- Сортировка элементов
- Соответствие понятий
- Интерактивные задания

### Управление квизами

В разделе **"Мои квизы"** вы можете:
- 📝 Редактировать квизы
- 📊 Просматривать статистику
- 🗑️ Удалять квизы
- 📋 Копировать квизы
- 🔗 Делиться ссылками

### Прохождение квиза

1. Выберите квиз в разделе **"Обзор"**
2. Нажмите **"Пройти тест"**
3. Отвечайте на вопросы по порядку
4. Используйте кнопки **"Назад"** и **"Далее"**
5. Завершите тест кнопкой **"Завершить"**
6. Просмотрите **детальные результаты**

## 🏗️ Архитектура проекта

### Структура данных

#### User (Пользователь)
```javascript
{
  id: Number,
  name: String,
  email: String,
  password: String,
  createdAt: ISO Date
}
```

#### Quiz (Квиз)
```javascript
{
  id: Number,
  title: String,
  description: String,
  category: String,
  authorId: Number,
  authorName: String,
  status: 'draft' | 'published',
  timeLimit: Number,
  shuffleQuestions: Boolean,
  showResults: Boolean,
  allowRetake: Boolean,
  questions: Array<Question>,
  createdAt: ISO Date
}
```

#### Question (Вопрос)
```javascript
{
  id: Number,
  type: 'single' | 'multiple' | 'text' | 'drag',
  text: String,
  options: Array<Option>, // для single/multiple
  correctAnswer: String // для text
}
```

#### Option (Вариант ответа)
```javascript
{
  id: Number,
  text: String,
  isCorrect: Boolean
}
```

#### Result (Результат)
```javascript
{
  id: Number,
  quizId: Number,
  userId: Number,
  score: Number,
  correctAnswers: Number,
  totalQuestions: Number,
  answers: Array,
  completedAt: ISO Date
}
```

### Хранилище данных (localStorage)

Все данные сохраняются в localStorage браузера:
- `users` - массив пользователей
- `currentUser` - текущий пользователь
- `quizzes` - массив всех квизов
- `quizResults` - массив результатов прохождений

### Основные классы и методы

#### QuizApp
Главный класс приложения:
```javascript
- init() - инициализация
- login(email, password) - вход
- register(name, email, password) - регистрация
- logout() - выход
- saveQuiz(quiz) - сохранение квиза
- getQuizzes() - получение квизов
- takeQuiz(quizId) - прохождение квиза
- finishQuiz() - завершение квиза
- showResults() - отображение результатов
```

## 🎨 Дизайн и стилизация

### Цветовая схема
```css
--primary: #00d4ff (Неоновый голубой)
--secondary: #ff00ea (Неоновая пурпурная)
--background: #0a0e17 (Темный фон)
--surface: #1a1f2e (Поверхности)
--text: #ffffff (Белый текст)
--text-secondary: #a0aec0 (Серый текст)
```

### Типографика
- **Display**: Playfair Display (заголовки)
- **Body**: DM Sans (основной текст)
- **Mono**: Space Mono (код, номера)

### Анимации
- Fade In при загрузке страниц
- Slide Up для модальных окон
- Float для карточек
- Smooth transitions для всех интерактивных элементов

## 🔒 Безопасность

### Реализованные меры:
1. **Валидация форм** на клиенте
2. **Проверка прав доступа** перед операциями
3. **Санитизация данных** перед сохранением
4. **Хеширование паролей** (в production версии рекомендуется)

### Рекомендации для production:

```javascript
// Добавьте на бэкенде:
- JWT аутентификацию
- Хеширование паролей (bcrypt)
- Rate limiting
- CSRF защиту
- XSS защиту
- SQL injection защиту
```

## 🔧 Настройка и кастомизация

### Изменение темы

Отредактируйте CSS переменные в `styles.css`:
```css
:root {
  --primary: #your-color;
  --secondary: #your-color;
  --background: #your-color;
}
```

### Добавление новых категорий

В `app.js` найдите объект `categoryNames` и добавьте новые:
```javascript
const categoryNames = {
  'education': 'Образование',
  'entertainment': 'Развлечения',
  'your-category': 'Ваша категория'
};
```

### Изменение лимитов

```javascript
// В app.js измените:
const MAX_QUESTIONS = 50; // максимум вопросов
const MIN_OPTIONS = 2; // минимум вариантов ответа
const MAX_OPTIONS = 10; // максимум вариантов ответа
```

## 📊 База данных (для production)

### Схема PostgreSQL

```sql
-- Пользователи
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Квизы
CREATE TABLE quizzes (
  id SERIAL PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  category VARCHAR(50),
  author_id INTEGER REFERENCES users(id),
  status VARCHAR(20) DEFAULT 'draft',
  time_limit INTEGER,
  shuffle_questions BOOLEAN DEFAULT false,
  show_results BOOLEAN DEFAULT true,
  allow_retake BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Вопросы
CREATE TABLE questions (
  id SERIAL PRIMARY KEY,
  quiz_id INTEGER REFERENCES quizzes(id) ON DELETE CASCADE,
  type VARCHAR(20) NOT NULL,
  text TEXT NOT NULL,
  correct_answer TEXT,
  position INTEGER,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Варианты ответов
CREATE TABLE options (
  id SERIAL PRIMARY KEY,
  question_id INTEGER REFERENCES questions(id) ON DELETE CASCADE,
  text TEXT NOT NULL,
  is_correct BOOLEAN DEFAULT false,
  position INTEGER
);

-- Результаты
CREATE TABLE results (
  id SERIAL PRIMARY KEY,
  quiz_id INTEGER REFERENCES quizzes(id),
  user_id INTEGER REFERENCES users(id),
  score INTEGER,
  correct_answers INTEGER,
  total_questions INTEGER,
  answers JSONB,
  completed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Схема MongoDB

```javascript
// Users
{
  _id: ObjectId,
  name: String,
  email: String,
  passwordHash: String,
  createdAt: Date
}

// Quizzes
{
  _id: ObjectId,
  title: String,
  description: String,
  category: String,
  authorId: ObjectId,
  status: String,
  timeLimit: Number,
  settings: {
    shuffleQuestions: Boolean,
    showResults: Boolean,
    allowRetake: Boolean
  },
  questions: [{
    type: String,
    text: String,
    options: [{
      text: String,
      isCorrect: Boolean
    }],
    correctAnswer: String
  }],
  createdAt: Date
}

// Results
{
  _id: ObjectId,
  quizId: ObjectId,
  userId: ObjectId,
  score: Number,
  correctAnswers: Number,
  totalQuestions: Number,
  answers: Array,
  completedAt: Date
}
```

## 🚀 Backend API (Node.js/Express пример)

```javascript
const express = require('express');
const app = express();

// Аутентификация
app.post('/api/auth/register', registerController);
app.post('/api/auth/login', loginController);
app.post('/api/auth/logout', logoutController);

// Квизы
app.get('/api/quizzes', getQuizzes);
app.get('/api/quizzes/:id', getQuizById);
app.post('/api/quizzes', authMiddleware, createQuiz);
app.put('/api/quizzes/:id', authMiddleware, updateQuiz);
app.delete('/api/quizzes/:id', authMiddleware, deleteQuiz);

// Результаты
app.post('/api/quizzes/:id/submit', submitQuiz);
app.get('/api/quizzes/:id/results', getQuizResults);
app.get('/api/results/:id', getResultById);

// Экспорт
app.get('/api/quizzes/:id/export', exportResults);
```

## 📈 Масштабирование

### Для увеличения производительности:

1. **Кеширование**:
   - Redis для сессий
   - Кеш результатов квизов
   - CDN для статических файлов

2. **База данных**:
   - Индексы на часто запрашиваемые поля
   - Репликация для чтения
   - Шардинг для больших объемов

3. **Frontend**:
   - Lazy loading изображений
   - Code splitting
   - Service Worker для офлайн работы

4. **Backend**:
   - Load balancing
   - Микросервисная архитектура
   - Message queues для тяжелых операций

## 🧪 Тестирование

### Рекомендуемые тесты:

```javascript
// Unit тесты
- Валидация форм
- Подсчет результатов
- Форматирование данных

// Integration тесты
- API endpoints
- База данных
- Аутентификация

// E2E тесты
- Создание квиза
- Прохождение квиза
- Просмотр результатов
```

## 📝 TODO и будущие улучшения

- [ ] Добавить тип вопроса с перетаскиванием
- [ ] Реализовать экспорт в PDF
- [ ] Добавить графики и charts для статистики
- [ ] Мультиязычность (i18n)
- [ ] Темы оформления (светлая/темная)
- [ ] Интеграция с Google Classroom
- [ ] Webhook уведомления
- [ ] API для интеграций
- [ ] Мобильное приложение
- [ ] Геймификация (достижения, рейтинги)

## 🐛 Известные ограничения

1. **localStorage** имеет лимит ~5-10MB
2. Нет синхронизации между вкладками
3. Данные локальные (не синхронизируются между устройствами)
4. Нет защиты от потери данных при очистке браузера

## 💡 Советы по использованию

1. **Регулярно экспортируйте данные** для backup
2. **Тестируйте квизы** перед публикацией
3. **Используйте понятные названия** категорий
4. **Добавляйте описания** к квизам
5. **Проверяйте правильность ответов** перед публикацией

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте документацию
2. Откройте консоль браузера (F12) для ошибок
3. Очистите localStorage и перезагрузите страницу
4. Проверьте совместимость браузера

## 📄 Лицензия

MIT License - свободное использование в личных и коммерческих проектах

## 🎉 Заключение

QuizMaster - это полнофункциональное решение для создания и прохождения квизов. Проект демонстрирует современные веб-технологии и может быть легко расширен под ваши нужды.

Удачи в создании квизов! 🚀
